Drop 64x64 PNG game icons here, named to match each game's `icon` path in js/data.jsx:

  rush-to-race-icon.png
  turbo-vertigo-icon.png

If a file is missing or fails to load, the game card automatically falls back
to the inline SVG glyph set in `iconFallback` (see GameThumbIcon in js/pages.jsx).
